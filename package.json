{
  "name": "testing-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "../messageflow copy/node_modules/.bin/concurrently \\\"yarn workspace app dev\\\" \\\"yarn workspace docs dev\\\"",
    "start": "yarn workspaces run start",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "A monorepo for multiple React applications",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
